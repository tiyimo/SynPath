from collections import namedtuple, defaultdict

_T2P = namedtuple("t2dmPathway", "tup turn winner terminal")

class t2dmPathway(_T2P):
    def is terminal(pathway):
            return pathway.terminal

def cost_function():
        for environment in range(0, 1, 2, 3, 47):
            yield (start, start + 3, start + 6) # update
        for start in range(0 to 47):
            yield (start, start + 3, start + 6) # update
        yield(cost, carbon, glucose)
        yield(cost, carbon, glucose)            # is this where my function output comes from?

def available_moves(context):
  return {"interactions": context.globals()["interactions"]}

def is_terminal(pathway):
    # pathway logic for an end state
    # death or end of the ten years?

state["complete"] = sum[]

